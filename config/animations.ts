// Animation & Performance Settings
export const animations = {
  // Page transition speeds (in milliseconds)
  pageTransition: 300,
  modalTransition: 200,
  cardHover: 150,
  
  // Scroll animation speeds
  scrollReveal: 400,
  scrollRevealDelay: 100,
  
  // Component animation speeds
  fadeIn: 300,
  slideIn: 400,
  scaleIn: 300,
  
  // Stagger delays (for lists)
  staggerDelay: 50,
  
  // Hover effects
  hoverScale: 1.05,
  hoverDuration: 200,
  
  // Route drawing animation
  routeDrawDuration: 3000,
  routeDrawDelay: 500,
  
  // Glitch effect
  glitchDuration: 100,
  
  // Map pin bounce
  pinBounceDuration: 2000,
}
